# === Mobile Mirroring Panel ===
base.url=https://magang.dikahadir.com/absen/login
dashboard.url=https://magang.dikahadir.com/apps/absent

# === Browser ===
browser=chrome

# === Headless Mode ===
headless=true

# === Timeouts (seconds) ===
implicit.wait=0
standard.wait=15
short.wait=5
page.load.timeout=30

# === Window ===
browser.maximize=true
